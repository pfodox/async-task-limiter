{
  "name": "async-task-limiter",
  "version": "0.0.0",
  "description": "limit for concurent async tasks",
  "main": "index.js",
  "scripts": {
    "build": "rm -rf ./pack && tsc && cp -rf CHANGELOG.md README.md package.json pack",
    "test": "jest"
  },
  "author": "andrey mikhaylov",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^14.11.2",
    "jest": "^26.4.2",
    "ts-jest": "^26.4.1",
    "ts-node": "^9.0.0",
    "typescript": "^4.0.3"
  }
}
